import { COPY } from "@/lib/copy";

/** Placeholder until 6.3 mission board. */
export default function MissionsPage() {
  return (
    <main className="stack">
      <h1>{COPY.missionsTitle}</h1>
      <p className="muted">Region map and open jobs.</p>
    </main>
  );
}
