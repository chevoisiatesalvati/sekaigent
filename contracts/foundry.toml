[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
ffi = false

[rpc_endpoints]
mainnet = "${OG_RPC_URL}"

[etherscan]
# chainscan may not use standard etherscan API; verify manually via https://chainscan.0g.ai

# 0G Mainnet (Aristotle) — chain ID 16661
# RPC default: https://evmrpc.0g.ai
